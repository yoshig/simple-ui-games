(function (root) {
  var SG = root.SG = (root.SG || {});

	var View = SG.View = function(el) {
		var $el = el
	};

	View.prototype.start = function() {
		var board = new SG.Board();

		$(window).
	};


	})(this)